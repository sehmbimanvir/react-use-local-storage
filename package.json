{
  "name": "uselocalstorage",
  "version": "1.0.0",
  "description": "A React Hook to persist data in localStorage",
  "main": "dist/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "rollup -c -w"
  },
  "author": "Manvir Singh",
  "license": "ISC",
  "peerDependencies": {
    "react": ">=16.8.1"
  },
  "devDependencies": {
    "rollup": "^2.66.1",
    "rollup-watch": "^4.3.1"
  }
}
